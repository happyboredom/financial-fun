/**
*
* EmailText
*
*/

import React from 'react';
// import styled from 'styled-components';


class EmailText extends React.PureComponent { // eslint-disable-line react/prefer-stateless-function
  render() {
    return (
      <div>abc
      </div>
    );
  }
}

EmailText.propTypes = {

};

export default EmailText;
